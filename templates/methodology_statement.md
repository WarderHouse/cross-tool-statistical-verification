All statistical analyses were conducted in Python ({python_version}) using {python_libs}, with code-generation assistance. To ensure the accuracy and reproducibility of results, a six-phase verification protocol was applied using the open-source crossverify harness (v{tool_version}): (a) the raw data were inspected against the original source file before analysis; (b) all data transformations were checked for range and structural integrity; (c) internal consistency checks were applied to every reported statistic; (d) selected statistics were recomputed directly from the raw data as independent spot-checks; (e) the analysis was re-run to confirm identical results ({seed}); and (f) the principal results were independently replicated in R ({r_version}), with {n_matched} of {n_compared} statistics matching the Python implementation within tolerance ({tolerance_desc}). The verification log, cross-tool comparison table, and analysis code are available as supplementary materials.

---
_Generated {date} for: {analysis_name}. This is a template — adapt the wording to your manuscript and confirm every clause is accurate for your study before submission._
